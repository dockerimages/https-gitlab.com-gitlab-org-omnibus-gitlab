#!/bin/sh
exec 2>&1
<%= render("mount_point_check.erb", cookbook: 'gitlab') %>
exec chpst -P -U <%= @options[:user] %>:<%= @options[:groupname] %> -u <%= @options[:user] %>:<%= @options[:groupname] %> /opt/gitlab/embedded/bin/patroni <%= File.join(node['patroni']['config_directory'], "patroni.yml") %>
