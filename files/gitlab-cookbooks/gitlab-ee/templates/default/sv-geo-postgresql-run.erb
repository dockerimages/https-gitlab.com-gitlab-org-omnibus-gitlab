#!/bin/sh
exec 2>&1
<%= render('mount_point_check.erb') %>
exec chpst -P \
  -U <%= @options[:username] %>:<%= @options[:groupname] %> \
  -u <%= @options[:username] %>:<%= @options[:groupname] %> \
  /opt/gitlab/embedded/bin/postgres -D <%= @options[:directory] %>
