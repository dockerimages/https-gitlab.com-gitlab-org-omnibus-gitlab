#!/bin/sh
exec 2>&1
<%= render('mount_point_check.erb') %>
exec chpst -P -U <%= node['postgresql']['username'] %> -u <%= node['postgresql']['username'] %> /opt/gitlab/embedded/bin/postgres -D <%= File.join(node['gitlab']['geo-postgresql']['dir'], 'data') %>
