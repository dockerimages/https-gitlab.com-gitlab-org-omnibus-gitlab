#!/bin/sh
set -e # fail on errors

# Redirect stderr -> stdout
exec 2>&1

cd <%= @options[:working_dir] %>

exec chpst -P \
-U <%= @options[:user] %> \
-u <%= @options[:user] %> \
<%= File.join(@options[:working_dir], 'bin', 'geo_log_cursor') %>
