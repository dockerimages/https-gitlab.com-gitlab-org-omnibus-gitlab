#!/bin/sh
  
exec 2>&1

echo "received HUP from runit, sending HUP to instances"

exec /opt/gitlab/embedded/bin/sv h <%= @options[:number_of_instances].times.map { |i| "./instances/pgbouncer-#{i}" }.join(' ') %>
