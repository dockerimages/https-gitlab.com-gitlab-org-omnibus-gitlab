#!/bin/sh
exec 2>&1

cd <%= @options[:dir] %>

exec /opt/gitlab/embedded/bin/chpst -P /usr/bin/env \
  dir=<%= @options[:dir] %> \
  pre_sleep=<%= @options[:pre_sleep] %> \
  post_sleep=<%= @options[:post_sleep] %> \
  /opt/gitlab/embedded/bin/gitlab-logrotate-wrapper
