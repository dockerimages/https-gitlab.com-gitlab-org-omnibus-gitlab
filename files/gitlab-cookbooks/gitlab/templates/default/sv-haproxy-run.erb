#!/bin/bash
set -e # fail on errors

# Redirect stderr -> stdout
exec 2>&1

cd <%= node['gitlab']['haproxy']['dir'] %>

exec chpst -P \
  -U <%= node['gitlab']['haproxy']['username'] %> \
  -u <%= node['gitlab']['haproxy']['group'] %> \
  /opt/gitlab/embedded/sbin/haproxy -f haproxy.cfg
